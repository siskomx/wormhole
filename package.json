{
  "name": "wormhole",
  "version": "0.12.2",
  "private": true,
  "type": "module",
  "description": "Evidence-aware planning state for AI coding agents.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "benchmarks:run": "tsx scripts/run-benchmarks.ts",
    "benchmarks:validate": "tsx scripts/validate-benchmarks.ts",
    "test": "vitest run tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "bin": {
    "wormhole-mcp": "./dist/src/cli.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "tree-sitter": "0.25.0",
    "tree-sitter-c-sharp": "0.23.5",
    "tree-sitter-javascript": "0.25.0",
    "tree-sitter-python": "0.25.0",
    "tree-sitter-rust": "0.24.0",
    "tree-sitter-typescript": "0.23.2",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
